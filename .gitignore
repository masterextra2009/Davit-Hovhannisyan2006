node_modules/
build/
dist/
sourcemaps/
coverage/
.DS_Store
*.log
.env*
!.env.example
.claude/settings.local.json
sever18-backup-*.json
scripts/.tmp-prerender-landing-bundle.cjs
.worktrees/
# Пароли от сервера. Строк этих одно время не было в боевой ветке, и обычный
# git add -A молча утягивал оба файла в коммит — а репозиторий публичный.
# $apr1$ в .htpasswd вскрывается перебором, ftp_pass.txt вообще лежит
# открытым текстом. Не удалять.
deploy/ftp_pass.txt
deploy/.htpasswd
