# Итог дня 2026-08-14 — на чём остановились

## Ждёт "да" на публикацию (единственное открытое действие)

**Коммит `c6f2af1`** — исправление `.github/workflows/deploy.yml`: публикация
раньше удаляла `package-lock.json` и ставила библиотеки заново "с нуля" при
каждом деплое, из-за чего версии (в первую очередь Firebase) незаметно
отличались от того, что реально тестировалось перед выкладкой. Заменено на
`npm ci` — ставит ровно те версии, что зафиксированы и закоммичены в
`package-lock.json`.

Закоммичено локально, **НЕ запушено**. Больше в коде сайта ничего не меняет.

**Следующий шаг:** дождаться "да" от Давида и сделать `git push origin
master:main` (обычный способ), проверить статус в GitHub Actions.

## Уже сделано и опубликовано сегодня (не путать с "в процессе")

- Гостевой заказ без обязательной регистрации — 5 задач, задеплоено.
- Кнопка "Войти через Telegram" — переведена на redirect-режим без eval,
  задеплоено (`f6a995a`).
- Декоративная заставка при открытии сайта — убрана ещё раньше сегодня
  (`2e9343c`), задеплоено. Давид должен был перепроверить в чистом окне —
  подтверждения от него по этому пункту пока не было.
- **Замена анимаций на заглавной странице (motion → CSS)** — это тоже уже
  полностью сделано и опубликовано (`9390c8b`), а не "в процессе", как
  могло показаться. Реальная экономия подтверждена: ≈123 КБ несжатого /
  ≈40 КБ сжатого на главном файле, при равных версиях библиотек.

## Важная находка (причина сегодняшнего расхождения в цифрах)

Давид заметил, что вес файла на сайте больше, чем я показывал в отчёте.
Причина — не код, а описанный выше баг публикации (плавающие версии
библиотек). Firebase на момент проверки был 12.14.0 (то, что тестировал
я), а реально задеплоенная сборка подтянула 12.17.1 — это одно
отличие добавило ~200 КБ. Полностью воспроизведено и подтверждено
байт-в-байт. Коммит `c6f2af1` выше — исправление именно этого.

## Не трогать без отдельной просьбы

`firestore.rules` и `functions/index.js` — доработка надёжности
телеграм-уведомлений в чат, до сих пор просто лежит незакоммиченной в
рабочей копии, отдельная задача, не связана с сегодняшним днём.

## Быстрый старт завтра

1. Спросить Давида "да" на публикацию `c6f2af1` → запушить → проверить Actions.
2. Спросить, проверил ли он заставку в чистом окне (пункт из вчерашнего плана).
3. Дальше — по его указанию.
